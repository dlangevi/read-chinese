package backend

import (
	"testing"
	// "path"
	// "os"
)

func TestImageSearch(t *testing.T) {
	//  imageClient := NewImageClient()
	// tmpMetaData := path.Join(os.TempDir(), "metadatabonk.json")
	//  LoadMetadata(tmpMetaData)
	//
	//  userSettings.AzureImageApiKey = "secret"
	//
	//  images, err := imageClient.SearchImages("fish")
	//  if err != nil {
	//    t.Errorf("Failed search %v", err)
	//  }
	//  if len(images) != 5 {
	//    t.Errorf("Wrong search %v", images)
	//  }
}
